export default function hotelFilterService(arrayToFilter, filterValue) {
    //something like results = arrayToFilter.filter(valueInArrayToFilter.hotelStaticContent.name => '?contains?' filterValue)


    /** or something like
     * let results = []
     * for(let i = 0; i < arrayToFilter.length; i++){
     *     if(arrayToFilter[i].hotelStaticContent.name '?contains?' filterValue){
     *          results.push(arrayToFilter[i])
     * }
     * }
     * if (!results.length){
     *      results = arrayToFilter.unshift({
     *          some sort of object that looks like a hotel?
     * })
     * }
     *
     */
    return(
        <>
        </>
    )
}
